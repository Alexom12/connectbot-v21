version: '3.8'

networks:
  test-net:
    driver: overlay

services:
  service-a:
    image: alpine:latest
    command: ["sleep", "3600"]
    networks:
      - test-net

  service-b:
    image: alpine:latest
    command: ["sleep", "3600"]
    networks:
      - test-net
