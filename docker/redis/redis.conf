bind 0.0.0.0
port 6379
# In this test/dev environment we disable protected-mode so the service
# can be accessed from other containers/host. For production use, enable
# protected-mode and secure with AUTH / firewall / TLS.
protected-mode no
dir /data
appendonly no
# Optional limits
maxmemory 256mb
maxclients 10000
