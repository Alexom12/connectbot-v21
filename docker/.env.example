## Example environment variables for local development / docker compose
# Copy this file to the repository root as `.env` and fill real secrets/URLs.
# Do NOT commit real secrets to git.

# Service auth token used by services to authenticate between each other
SERVICE_AUTH_TOKEN=changeme123

# Base URL of the Data API used by matching-service
# In compose this defaults to http://web:8000
MATCHING_DATAAPI_URL=http://web:8000

# Telegram bot token (keep secret)
TELEGRAM_BOT_TOKEN=123456:ABC-DEF

# Optional token to trigger metrics/push workflows
METRICS_TRIGGER_TOKEN=

# Additional overrides
# Example: override JAVA_OPTIONS or SPRING profiles
#JAVA_OPTS=-Xmx512m
# Example .env for docker-compose
METRICS_TRIGGER_TOKEN=dev-token-please-change
